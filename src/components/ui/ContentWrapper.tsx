import styled from 'styled-components';

// This is needed to always push the footer to the bottom.
// Make sure it is on the same level as the footer.
export const ContentWrapper = styled.div`
  flex: 1 0 auto;
`;
